# CheckCheck: 智能文本比对工具

CheckCheck 是一款基于OCR（光学字符识别）技术的智能桌面应用程序，旨在帮助用户高效准确地比对图像中的两组文本内容。无论是生产线上的标签核对、文档校对，还是任何需要快速比较视觉文本信息的场景，CheckCheck 都能提供强大的支持。

## 核心功能

1.  **双模式文本识别与比对：**
    *   **静态图片模式：** 用户可以上传本地图片，应用程序将自动识别图片中的两组预设文本（“字符串1”和“字符串2”，通常对应标牌文字和喷码文字），并进行比对。
    *   **实时相机模式：** 支持连接和切换多个摄像头，实时捕捉视频流，并在捕获的帧上进行文本识别与比对。

2.  **智能文本区分与高亮：**
    *   应用程序能够识别图像中不同区域的文本，并使用不同颜色（如绿色代表“字符串1”，红色代表“字符串2”）在图像上绘制文本框，直观显示识别区域。
    *   比对结果清晰展示相似度百分比，并以“通过 (✔)”或“不通过 (✘)”（100%相似度为通过）的形式呈现。
    *   在结果展示区域，使用HTML高亮（红/绿色、加粗）显示两组文本之间的差异，方便用户快速定位不同之处。

3.  **历史记录与追溯：**
    *   所有识别和比对结果（包括原始图片路径、识别文本、比对结果、时间戳）都会自动保存到本地数据库。
    *   提供历史记录查看窗口，用户可以：
        *   浏览历史比对详情。
        *   根据“标牌文字”或“喷码文字”进行关键词搜索（高亮匹配项）。
        *   根据比对结果（“全部”、“通过”、“不通过”）进行筛选。

4.  **用户友好的界面与交互：**
    *   简洁直观的图形用户界面 (GUI)，易于上手。
    *   支持动态切换识别模式（图片/相机/恢复相机）。
    *   识别结果、文本框和置信度在图像上清晰展示，文本框背景优化以提高可读性。
    *   操作过程中的状态反馈（如按钮禁用、状态文本更新）。
    *   提供清空识别结果、恢复相机画面等便捷操作。

## 主要特点

*   **高精度OCR引擎：** 基于先进的OCR技术（如PaddleOCR），确保文本识别的准确性。
*   **灵活的文本比对策略：** 文本预处理机制，可配置忽略特定字符（如中文、空格、标点符号），专注于核心内容的比较。
*   **稳健的摄像头管理：** 支持多摄像头选择，优化了摄像头启停和切换逻辑，避免了常见的闪退问题。
*   **持续优化与迭代：** 项目经历了多次功能增强和问题修复，包括UI美化、错误处理、性能提升等，致力于提供稳定可靠的用户体验。
*   **易于部署和使用：** 提供必要的环境配置脚本（如 `install_env.bat`），方便用户搭建运行环境。

## 技术栈概览

*   **主要语言：** Python
*   **GUI框架：** PyQt5 / PySide
*   **OCR引擎：** PaddleOCR (ch_PP-OCRv4系列模型)
*   **图像处理：** OpenCV (cv2)
*   **数据库：** SQLite (用于存储历史记录)

## 应用场景

CheckCheck 特别适用于以下场景：

*   **工业生产：** 产品标签、喷码、序列号等信息的快速核对，防止错漏。
*   **文档管理：** 纸质文档电子化过程中的文本校对。
*   **物流仓储：** 包裹标签、货单信息的比对。
*   **任何需要快速、准确比对图像中文本的场合。**

CheckCheck 通过其强大的功能和友好的用户体验，旨在成为您文本比对任务中不可或缺的助手。
