## 实施计划（已转向图号识别与复制）

- 核心目标：去除比对，仅识别 MAIN/HEAD；MAIN 自动复制；UI 与交互按新规范重构

### 已完成
- 相机完整显示（等比例，不裁剪、不拉伸），默认更大窗口高度
- 实时识别与单次识别模式；Enter/中键触发
- 识别结果图预览（右侧）；绿色框，MAIN 加粗，ASCII 标签（MAIN/HEAD/TIME），更小字号与线宽
- 文本抽取与规则：
  - HEAD：`^[A-Z]{1,3}\d{2,4}$`
  - MAIN 严格/兜底；大写化、去空格、移除尾点、限定字符集
- 状态提示色：绿/黄/红，仅作用于状态栏
- 声音：首次播放预热修复
- 历史记录：多选删除；新增“删记录+文件”
- 移除：上传图像、切换到图片、设置页面/按钮

### 待验证/优化
- 长时间连续实时识别稳定性
- 少数相机驱动在退出时线程销毁警告的彻底解决

### 交付
- 提供 conda-pack 环境包与 `start_with_conda.bat`